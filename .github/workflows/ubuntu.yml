name: ubuntu ci

on:
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: ubuntu:26.04

    steps:
    - name: install boost
      run: apt-get update && apt-get install -y libboost-all-dev expect
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    - name: configure
      run: mkdir -p build && cd build && cmake -DCMAKE_CXX_FLAGS="-Werror -fsanitize=address,undefined -fno-omit-frame-pointer"  -DCMAKE_BUILD_TYPE=Release ..
    - name: run test harness
      run: cd build && ../scripts/test_all.sh -j4 -l -t0s